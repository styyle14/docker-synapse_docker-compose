version: '2'
services:
   synapse:
      build:
         context: ./build/docker-synapse
         dockerfile: Dockerfile
         args:
            username: "synapse"
      image: synapse:latest
      ports:
         - "8448:8448"
      volumes:
         - "./run/config:/home/synapse/config"
